Input is seperated by ',' and the range is seperated by '-'

Functions TO:
1. Seperate the input into ranges -> Done
2. Seperate the ranges into 2 numbers -> Done
3. A function to check if the ID is invalid and store it. -> Done
  - It happens only if the length is even.
  - Lets take 
4. Sum the invalid Id
